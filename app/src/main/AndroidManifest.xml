<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ragamfinal"
        tools:targetApi="31">

        <!-- Splash/Main Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Ragamfinal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Added user option login page -->
        <activity android:name=".UserOptionLoginActivity" />
        <!-- Optionally add other activities below -->
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignInActivity" />

        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".OtpVerificationActivity" />
        <activity android:name=".OtpVerificationActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".StudentLoginActivity" />
        <activity android:name=".InstructorLoginActivity" />
        <activity android:name=".StudentSignupActivity" />
        <activity android:name=".InstructorSignupActivity" />

    </application>

</manifest>
